#include <stdio.h>
#include <conio.h>
#include "TypingPracticeCallback.h"

int getTypingSpeedCallback(CALLBACK pCallback,int n) 
{ 
   return pCallback(n); 
}

int getTypingSpeedCallback(CALLBACK pCallback,int n) 
{ 
   return pCallback(n); 
}